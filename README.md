Repo za laboratorijske vježbe iz Osnova strojnog učenja

```
git config --global user.name "mhasenay"
git config --global user.email "mhasenay@gmail.com"
```
Postavljanje Tensorboard-a
```
py -m tensorboard.main --logdir=logs/cnn
```
Ako to ne radi:  
prebacit VS Code u stariju ver  
py -m pip install setuptools == 69.5.1
